# Dependencies
node_modules/

# Environment & secrets (never push real credentials)
.env
.env.*
!.env.example

# Build output
dist/
build/

# Logs & package manager debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Tests & coverage
coverage/
.nyc_output/

# OS
.DS_Store
Thumbs.db

# IDE / editor (commit .vscode/ only if the team shares launch/tasks/settings)
.idea/
.vscode/
*.swp
*.swo
*~

# Local SQLite / stray DB files
*.db
*.db-journal

# SSL / keys (if generated locally)
*.pem
*.key

# Personal / local-only (not part of the app)
.cursor
plantUML.txt
diagramClass.txt
planning.txt
postman/
seed